// function declaration with params and return typed value
fn main(int a, string b): int {
  // variable declaration
  int i = 4;
  string w = "test";
  var d;

  // binary expression
  4 + 6;
  1 - 2;
  9 / 8;
  4 * 2;

  // unary expression
  i++;
  ++i;
  -i;
  --i;
  i--;
  !i;

  // if else statement
  if (a == 4) {
    a = 3;  
  } else {
    a = 4; 
  }

  // function calling
  test(a, 5, "test");

  // return statement 
  return 0;
}

fn test(int a, int b, string c) {
  int a = 2;
}
